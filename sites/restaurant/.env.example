# 非推奨: 接続設定はリポジトリルートの .env に集約しました。
#
# cp ../../.env.example ../../.env
# でルート .env を作成し、DATABASE_URL / DEVREV_* 等をそこに記入してください。
#
# 既存の sites/restaurant/.env は互換のため読み込まれますが、
# 新規セットアップではルート .env のみを使ってください。
